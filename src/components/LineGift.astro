---
import { lineGift, mainCta } from "../data/site";
import CtaButton from "./CtaButton.astro";
---

<!-- このサイトの主導線。他のボタンより目立たせる -->
<section id="line" class="mt-14 scroll-mt-6">
  <div
    class="rounded-2xl border border-accent/40 bg-surface px-5 py-8 text-center sm:px-9 sm:py-10 shadow-[0_1px_3px_rgba(33,37,42,0.05)]"
  >
    <p class="text-[12px] tracking-[0.2em] text-accent">FREE GIFT</p>

    <h2 class="mt-3 text-[22px] leading-snug font-bold">
      {lineGift.heading}
    </h2>

    <p class="mt-4 text-[17px] font-bold text-accent">
      {lineGift.giftName}
    </p>

    <p class="mt-4 text-[15px] text-ink-muted">{lineGift.lead}</p>

    <ul class="mt-7 space-y-3 border-t border-hairline pt-7 text-left">
      {
        lineGift.benefits.map((benefit) => (
          <li class="flex gap-3 text-[14px]">
            <span class="text-accent" aria-hidden="true">
              ✓
            </span>
            <span>{benefit}</span>
          </li>
        ))
      }
    </ul>

    <div class="mx-auto mt-8 max-w-[380px]">
      <CtaButton href={mainCta.url} label={mainCta.label} />
      {
        mainCta.note && (
          <p class="mt-3 text-[12px] text-ink-muted">{mainCta.note}</p>
        )
      }
    </div>
  </div>
</section>
